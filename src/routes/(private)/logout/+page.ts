import { redirect } from '@sveltejs/kit';

/** @type {import('./$types').PageLoad} */
export async function load() {
    // const post = await logout(params.slug);

    // localStorage.removeItem('token');

    // throw redirect(300, '/login');
    return {}
}